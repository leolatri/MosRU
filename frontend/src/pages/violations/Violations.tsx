import { Typography } from "antd"

const Violations = () => {
    return (
        <div>
            <Typography.Title level={1}>Нарушения</Typography.Title>
        </div>
    )
};

export default Violations;